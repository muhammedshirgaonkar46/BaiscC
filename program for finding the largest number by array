
#include <stdio.h>
main()
{
    int a[5]={4,8,9,10};
    int large=a[0];
    int i;
    
    for(i=1;i<=4;i++)
    {
        if(a[i]>large)
        {
            large=a[i];
        }
    }
    printf("largest number is %d",large);
}
