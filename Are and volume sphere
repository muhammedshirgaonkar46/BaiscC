#include<stdio.h>

main()
{
    const float pi=3.14;
    int r;
    float area;
    float volume;
    printf("enter radius r=");
    scanf("%d",&r);
    area=4*pi*r*r;
    volume=(4/3)*pi*r*r*r;
    printf("area=%f and volume=%f", area,volume);
    
}
    
