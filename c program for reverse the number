#include <stdio.h>
main()
{
    int i,n,q;
    int rem,rev=0;
    printf("enter any four digit number=");
    scanf("%d",&n);
    for(i=1;n>0;i++)
    {
        rem=n%10;
        rev=(rev*10)+rem;
        q=n/10;
        n=q;
    }
    printf("reverse number=  %d ",rev);
    
}
